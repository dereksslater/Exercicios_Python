#Calcular e apresentar a área do triângulo a seguir obtendo os valores para base (b) e altura (h) via entrada do usuário (os valores podem ser reais ). Apresente mensagens para obtenção das entradas e para a saída. 

base = float(input("Digite o valor da base: "))

altura = float(input("Digite o valor da altura: "))


resultado = (base * altura) /2

print(f" o valor da Área é {resultado} ")

